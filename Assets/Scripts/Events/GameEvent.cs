namespace haw.pd20.events
{
    /// <summary>
    /// Empty base class for custom events.
    /// It is a marker class used to restrict the generic events passed in to the EventManager functions to have the same base class.
    /// </summary>
    public class GameEvent { }
}